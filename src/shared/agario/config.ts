export const MAP_WIDTH = 2000;
export const MAP_HEIGHT = 2000;

export const MAX_SPEED = 400;
export const MIN_SPEED = 50;

const MASS = 6;//TODO: update
export const MAXIMUM_MASS_LIMIT = 22500 * MASS;

export const INIT_RADIUS = 20;
export const ORB_RADIUS = 8;
export const INIT_MASS = Math.PI * INIT_RADIUS * INIT_RADIUS;
export const ORB_MASS = Math.PI * ORB_RADIUS * ORB_RADIUS;
export const MASS_FROM_ORBS = Math.PI * MASS * MASS;

export const MAX_ORBS = 200;
